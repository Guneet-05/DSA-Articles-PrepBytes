#include <bits/stdc++.h>
using namespace std;
string convertToString(char* a)
{
	string s(a);
	return s;
}

int main()
{
	char a[] = { 'a', 'e', 'i', 'o', 'u' };

	string s_a = convertToString(a);

	cout << s_a << endl;

	return 0;
}
